<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.h.customer_vendor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".customer_cp"></activity>
        <activity android:name=".view_lending" />
        <activity android:name=".view_payment" />
        <activity android:name=".view_sale" />
        <activity android:name=".customer_home" />
        <activity android:name=".customer_login" />
        <activity android:name=".add_payment" />
        <activity android:name=".add_sale" />
        <activity android:name=".add_customer" />
        <activity android:name=".vendor_home" />
        <activity android:name=".lending" />
        <activity android:name=".vendor_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>